[project]
name = "agio-broker"
version = "0.0.2"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
"agio_broker" = [
    "__agio__.yml",
]